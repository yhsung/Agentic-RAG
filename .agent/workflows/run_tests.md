---
description: Run the project's test suite using pytest
---

This workflow runs the automated tests to verify system integrity.

1. Run all tests
// turbo
```bash
source venv/bin/activate
pytest
```

2. Run with verbose output
// turbo
```bash
source venv/bin/activate
pytest -v
```
